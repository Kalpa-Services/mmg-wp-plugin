repos:
  - repo: local
    hooks:
      - id: phpcs
        name: PHP CodeSniffer
        entry: vendor/bin/phpcs
        args: ['-p', '--standard=phpcs.xml']
        language: system
        types: [php]
